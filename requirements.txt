# Environnement unique pour tout le projet (scripts racine + dashboard
# Streamlit) — c'est aussi le fichier utilisé par Streamlit Community Cloud
# pour déployer dashboard/app.py (voir dashboard/SECRETS.md). Plus de backend
# FastAPI séparé : tout tourne dans ce même environnement Python.

# === Cœur du pipeline (scraper -> lead_worker -> ceo_agent) ===
requests==2.32.3
beautifulsoup4==4.14.3
urllib3==2.2.3
python-dotenv==1.0.0
supabase==2.5.0

# === Dashboard (dashboard/app.py) ===
streamlit==1.38.0
pandas==2.2.2

# === Signature électronique + paiement (Yousign / Stripe) ===
stripe==10.12.0
fpdf2==2.7.9

# === Portail Client (authentification, dashboard/auth.py) ===
bcrypt==4.2.0
